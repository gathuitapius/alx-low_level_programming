#include "main.h"

/**
 * _islower -  checks for lowercase character
 * @d: contains value to be compared
 * Return: Always 0.
 */
int _islower(int d)

{

if (d > 'a' && d < 'z')
{
return (1);
}

else
{
return (0);
}

}
