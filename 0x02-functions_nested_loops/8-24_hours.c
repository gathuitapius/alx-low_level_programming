#include "main.h"
/**
 * jack_bauer - print every minute of the day, starting from 00:00 - 23:59.
 *
 * Return : void
 */

void jack_bauer(void)
{
	int hr0, hr1;
	int min0, min1;

	for (hr0 = 0; hr0 < 2;hr0++)
	{
		for (hr1 = 0; hr1 < 10; hr1++)
		{
			for (min0 = 0; min0 < 6; min0++)
			{
				 for (min0 = 0; min0 < 6; min0++)
		                        {
						_putchar('0' + hr0);
						_putchar('0' + hr1);
						_putchar(':');
						_putchar('0' + min0);
						_putchar('0' + min1);
						_putchar('\n');
						if (h0 == 2 && hr1 == 3 && min0 == 5 && min1 == 9
							{
								return;
							}
                		        }

			}
		}
	}
}
