#include "main.h"

/**
 * swap_int -  swaps the values of two integers.
 * @a: store pointer to a.
 * @b: store pointer to b.
 * Return: Always 0.
 */
void swap_int(int *a, int *b)
{
	int temp;

	*a = temp;
	*b = *a;
	temp = *b;

}
